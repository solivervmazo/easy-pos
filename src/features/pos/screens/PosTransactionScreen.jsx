import { StyleSheet, Text, View } from "react-native";
import React from "react";

const PosTransactionScreen = () => {
  return (
    <View>
      <Text>PosTransactionScreen</Text>
    </View>
  );
};

export default PosTransactionScreen;

const styles = StyleSheet.create({});
